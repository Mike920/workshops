%p#notice= notice
%p#error= flash[:error]

%h2= product.title
%p
  = product.description
%p
  %b Price:
  = product.price

%p
  %b Category
  = product.category.name

%h3 Reviews


= render reviews

= link_to 'Edit', edit_category_product_path(category.id, product.id)  if user_is_owner


%h3 Add a review

= render 'reviews/form'